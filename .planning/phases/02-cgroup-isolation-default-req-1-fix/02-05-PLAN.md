---
phase: 02-cgroup-isolation-default-req-1-fix
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - internal/session/userconfig.go
autonomous: true
requirements:
  - PERSIST-01
  - PERSIST-02
  - PERSIST-03
tags: [docs, comment-update, full-suite-confirmation]

must_haves:
  truths:
    - "The two example-config comment blocks in userconfig.go (around lines 875-878 and 1825-1828) describe the NEW default behavior accurately: 'true on Linux+systemd, false otherwise; explicit override always honored'."
    - "The full TestPersistence_ suite, run after this plan lands, shows TEST-01, TEST-02, TEST-03 GREEN and TEST-04 SKIPS on this Linux+systemd host. TEST-05..08 RED is expected (Phase 3 territory)."
    - "TestPersistence_ExplicitOptOutHonoredOnLinux (Plan 02) all three arms GREEN."
    - "TestLogCgroupIsolationDecision_ (Plan 03) all five tests GREEN."
    - "TestStartCommandSpec_FallsBackToDirect / BothFailWrapsError (Plan 04) GREEN."
  artifacts:
    - path: "internal/session/userconfig.go"
      provides: "Updated example comment blocks reflecting the new default semantics"
      contains: "true on Linux+systemd"
  key_links:
    - from: "user reading config.toml example comments"
      to: "actual GetLaunchInUserScope() default behavior"
      via: "comment matches code"
      pattern: "true on Linux\\+systemd"
---

<objective>
Update the two example-config comment blocks in `internal/session/userconfig.go` so they describe the NEW default behavior, and run the FULL persistence suite one final time to confirm the Phase 2 GREEN matrix on this Linux+systemd host.

Purpose: A documentation hole left by Plan 02 — the user-facing comment block at lines 875-878 still says "Default: false" even though we changed the default. Operators reading the example config would be misled. Plan 02 already partially updated the doc-comment on the struct field; this plan completes the alignment with the example block at line 1825-1828 and confirms the whole phase by running the full suite.

Output: One small comment-only edit to `internal/session/userconfig.go`, one full-suite test run captured in the SUMMARY, one closing commit.
</objective>

<execution_context>
@/home/ashesh-goplani/.claude/get-shit-done/workflows/execute-plan.md
@/home/ashesh-goplani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cgroup-isolation-default-req-1-fix/02-CONTEXT.md
@.planning/phases/02-cgroup-isolation-default-req-1-fix/02-01-PLAN.md
@.planning/phases/02-cgroup-isolation-default-req-1-fix/02-02-PLAN.md
@.planning/phases/02-cgroup-isolation-default-req-1-fix/02-03-PLAN.md
@.planning/phases/02-cgroup-isolation-default-req-1-fix/02-04-PLAN.md
@CLAUDE.md
@internal/session/userconfig.go
@internal/session/session_persistence_test.go

<interfaces>
<!-- Comment blocks to update -->

Block A — internal/session/userconfig.go around lines 875-878 (already partially updated by Plan 02 Task 2; confirm it now reads):
```go
	// LaunchInUserScope starts new tmux servers via `systemd-run --user --scope`
	// so the tmux server lives under the user's systemd manager instead of the
	// current login session scope. This keeps tmux alive when an SSH session
	// scope is torn down.
	//
	// Default (when nil / field absent): true on Linux hosts where
	// `systemd-run --user --version` succeeds, false otherwise. Explicit
	// `launch_in_user_scope = true` or `launch_in_user_scope = false` in
	// config.toml is always honored. Pointer type is required to distinguish
	// "field absent" from "explicit false".
	LaunchInUserScope *bool `toml:"launch_in_user_scope"`
```
If Plan 02 left this in the right shape, Task 1 below has nothing to do for Block A. Otherwise, restate it.

Block B — internal/session/userconfig.go around lines 1825-1828 (the user-facing example config comment block embedded in the default config string). This is what gets WRITTEN to a fresh `~/.agent-deck/config.toml` if the user requests an example. It currently reads:
```
# launch_in_user_scope starts new tmux servers with systemd-run --user --scope
# so they are not tied to the current login session scope (useful for SSH/tmux).
# Default: false
# launch_in_user_scope = true
```

Update Block B to:
```
# launch_in_user_scope starts new tmux servers with systemd-run --user --scope
# so they survive when the current login session is torn down (e.g. SSH logout).
# Default: true on Linux+systemd hosts where `systemd-run --user --version`
#          succeeds, false on macOS / BSD / Linux without a user manager.
# An explicit setting here is ALWAYS honored.
# launch_in_user_scope = false
```

Note the example value flipped from `= true` to `= false` — since the new default IS `true` on the target host, the example showing `= true` is now redundant. Showing `= false` demonstrates the override path, which is the case a user would actually edit the file for. Leave the line commented (#-prefixed) — we don't want to actively disable isolation by writing a fresh config.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update example-config comment blocks to match new default</name>
  <files>internal/session/userconfig.go</files>
  <read_first>
    - .planning/phases/02-cgroup-isolation-default-req-1-fix/02-CONTEXT.md ("Files to modify (authoritative list)" — last bullet covers both comment blocks)
    - internal/session/userconfig.go (current state of both blocks — confirm Plan 02 Task 2 already updated Block A; confirm Block B still has stale "Default: false")
    - The full file context around the example block (lines 1810-1845 from prior reads) — the example block sits inside a long string literal returned by some `getDefaultConfigString()`-like function. Edit only the lines bounded by the `# launch_in_user_scope` comments — do NOT touch surrounding TOML examples.
  </read_first>
  <action>
1. Read internal/session/userconfig.go around lines 870-900 and confirm Block A reads as the post-Plan-02 form documented above (`LaunchInUserScope *bool` + multi-paragraph default comment). If it does NOT (e.g. Plan 02 only changed the type and not the comment), restate it to match the AFTER form in the interfaces block above.

2. Read internal/session/userconfig.go around lines 1820-1830 and locate the `# launch_in_user_scope` comment block in the embedded example config. Replace lines 1825-1828 (the four `#`-prefixed lines plus the example) with the AFTER form documented in the interfaces block.

3. Run `go vet ./...` (exits 0 — comment-only changes), `go build ./...` (exits 0).

4. DO NOT modify the typo `/ scope is torn down` referenced in CONTEXT.md as out-of-scope. Leave any pre-existing typos alone.
  </action>
  <verify>
    <automated>grep -c "true on Linux+systemd" internal/session/userconfig.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "true on Linux" internal/session/userconfig.go` returns at least `2` (one in struct doc, one in example block)
    - `grep -c "Default: false$" internal/session/userconfig.go` returns `0` (the stale "Default: false" line is gone from the example block)
    - `grep -c "launch_in_user_scope = false" internal/session/userconfig.go` returns at least `1` (example value flipped)
    - `go vet ./...` exits 0
    - `go build ./...` exits 0
    - `git diff --stat internal/tmux/ internal/session/instance.go internal/session/storage.go cmd/agent-deck/session_cmd.go` returns empty
    - Commit signed `Committed by Ashesh Goplani`; `git log --format=%B HEAD~0..HEAD | grep -c "Co-Authored-By: Claude"` returns `0`
  </acceptance_criteria>
  <commit_message>
docs(02-05): align userconfig.go example comments with new default

Updates the two example-config comment blocks in
internal/session/userconfig.go to reflect the post-Plan-02 default
("true on Linux+systemd, false otherwise; explicit override always
honored") so the user-facing example matches the actual runtime
behavior. Flips the example line from `= true` to `= false` since
`true` is now the default and the override case is what users
actually edit the file for.

Phase 2 / Plan 05 — Task 1. Comment-only; no behavior change.

Committed by Ashesh Goplani
  </commit_message>
  <done>Both comment blocks updated, build clean, no behavior change, commit clean.</done>
</task>

<task type="auto">
  <name>Task 2: Final full-suite confirmation run + Phase 2 sign-off</name>
  <files>(no source modifications — verification + summary only)</files>
  <read_first>
    - .planning/phases/02-cgroup-isolation-default-req-1-fix/02-CONTEXT.md (Phase 2 in-scope vs out-of-scope test states)
    - .planning/ROADMAP.md Phase 2 success criteria 1-7
    - CLAUDE.md "Eight tests" mandate
  </read_first>
  <action>
1. Run the full persistence suite with verbose output and capture stdout+stderr to a temp file:
   ```bash
   go test -v -run TestPersistence_ ./internal/session/... -race -count=1 2>&1 | tee /tmp/phase2-final.log
   ```

2. Run the new helper suites:
   ```bash
   go test -v -run TestIsSystemdUserScopeAvailable_ ./internal/session/... -race -count=1 2>&1 | tee -a /tmp/phase2-final.log
   go test -v -run TestLogCgroupIsolationDecision_ ./internal/session/... -race -count=1 2>&1 | tee -a /tmp/phase2-final.log
   go test -v -run "TestStripSystemdRunPrefix_|TestStartCommandSpec_" ./internal/tmux/... -race -count=1 2>&1 | tee -a /tmp/phase2-final.log
   ```

3. Build the full project: `go build ./... 2>&1 | tee -a /tmp/phase2-final.log` — exit 0.

4. Verify no test tmux servers leaked: `tmux list-sessions 2>&1 | grep -c agentdeck-test-` should return `0`. If non-zero, document the leak in the SUMMARY (do NOT kill them blindly — use `tmux kill-server -t <exact-name>` per the 2025-12-10 mandate).

5. Decision-coverage check — for each of the seven phase requirements, confirm a passing test:
   - PERSIST-01 (Linux default true): TEST-03 GREEN
   - PERSIST-02 (non-systemd default false): TEST-04 SKIPS on this host (would PASS on a non-systemd host); document the matrix
   - PERSIST-03 (explicit override honored): TestPersistence_ExplicitOptOutHonoredOnLinux all 3 arms GREEN
   - PERSIST-04 (fresh install spawns under user@UID.service): TEST-01 GREEN proves it; if the SUMMARY also notes the systemctl --user status output for an agentdeck-tmux-*.scope unit, full coverage
   - PERSIST-05 (SSH logout doesn't kill server): TEST-01 GREEN simulates this
   - PERSIST-06 (graceful fallback): TestStartCommandSpec_FallsBackToDirect + BothFailWrapsError GREEN
   - OBS-01 (startup log line): TestLogCgroupIsolationDecision_ x5 GREEN + Plan 03 Task 3 wired the call site

6. Roadmap success-criteria check — for each of the seven Phase 2 success criteria from ROADMAP.md, mark proven / pending in the SUMMARY:
   1. Linux+systemd default true — proven by TEST-03
   2. Non-systemd default false — proven by TEST-04 logic (SKIPS here, validated on macOS)
   3. TEST-01 GREEN — proven
   4. TEST-02 GREEN — proven
   5. systemctl --user status shows agentdeck-tmux-*.scope — capture one fresh-install scope name and the show output as evidence
   6. grep 'tmux cgroup isolation' on log files returns one of the four exact strings — capture grep output
   7. Fallback never blocks — proven by TestStartCommandSpec_FallsBackToDirect

7. Write the Phase 2 sign-off SUMMARY at `.planning/phases/02-cgroup-isolation-default-req-1-fix/02-05-SUMMARY.md` (plan-level) AND a phase-level rollup if the project convention has one (check Phase 1 dir for a phase-level summary; Phase 1 only has plan-level summaries, so just produce the plan-level one here).

8. Commit with the message in `commit_message` below. Use `git add -f` for the SUMMARY because `.planning/` is excluded.
  </action>
  <verify>
    <automated>go test -run TestPersistence_ ./internal/session/... -race -count=1 2>&amp;1 | grep -E "^(---|=== RUN|PASS|FAIL|SKIP|ok)" | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/phase2-final.log` exists and contains all four test-runs' output
    - `grep -c "^--- PASS: TestPersistence_TmuxSurvivesLoginSessionRemoval" /tmp/phase2-final.log` returns at least `1` (TEST-01 GREEN)
    - `grep -c "^--- PASS: TestPersistence_TmuxDiesWithoutUserScope" /tmp/phase2-final.log` returns at least `1` (TEST-02 GREEN)
    - `grep -c "^--- PASS: TestPersistence_LinuxDefaultIsUserScope" /tmp/phase2-final.log` returns at least `1` (TEST-03 GREEN)
    - `grep -c "^--- SKIP: TestPersistence_MacOSDefaultIsDirect" /tmp/phase2-final.log` returns at least `1` (TEST-04 SKIPS — expected on Linux+systemd)
    - `grep -c "^--- PASS: TestPersistence_ExplicitOptOutHonoredOnLinux" /tmp/phase2-final.log` returns at least `1` (Plan 02 helper)
    - `grep -cE "^--- (FAIL|PASS): TestPersistence_(RestartResumesConversation|StartAfterSIGKILLResumesConversation|ClaudeSessionIDSurvivesHookSidecarDeletion|FreshSessionUsesSessionIDNotResume)" /tmp/phase2-final.log` returns `4` (TEST-05..08 result lines exist; FAIL is acceptable — Phase 3 territory)
    - `tmux list-sessions 2>&amp;1 | grep -c agentdeck-test-` returns `0`
    - `go build ./...` exits 0
    - `test -f .planning/phases/02-cgroup-isolation-default-req-1-fix/02-05-SUMMARY.md` returns 0
    - The SUMMARY contains a literal "Phase 2 sign-off" section and the seven requirement-status lines (PERSIST-01 to PERSIST-06 + OBS-01)
    - `git log --format=%B HEAD~0..HEAD | grep -c "Co-Authored-By: Claude"` returns `0`
    - Commit message ends `Committed by Ashesh Goplani`
  </acceptance_criteria>
  <commit_message>
docs(02-05): Phase 2 sign-off — REQ-1 fully GREEN on Linux+systemd

Final-confirmation run of TestPersistence_, TestIsSystemdUserScope-
Available_, TestLogCgroupIsolationDecision_, and the tmux fallback
suite. On this Linux+systemd executor host:

- TEST-01 TmuxSurvivesLoginSessionRemoval: GREEN
- TEST-02 TmuxDiesWithoutUserScope: GREEN
- TEST-03 LinuxDefaultIsUserScope: GREEN
- TEST-04 MacOSDefaultIsDirect: SKIPS (expected — no non-systemd host)
- TestPersistence_ExplicitOptOutHonoredOnLinux: GREEN (3/3 arms)
- TestIsSystemdUserScopeAvailable_*: 3/3 GREEN
- TestLogCgroupIsolationDecision_*: 5/5 GREEN
- TestStripSystemdRunPrefix_* + TestStartCommandSpec_*: 4/4 GREEN
- TEST-05..08: still RED (Phase 3 territory — REQ-2 fix)

Phase 2 requirements PERSIST-01..PERSIST-06 + OBS-01 all satisfied.
Plan-level SUMMARY captured at 02-05-SUMMARY.md with full test logs,
systemctl --user status evidence, and the observed log line.

Phase 2 / Plan 05 — Task 2. Phase 2 closed.

Committed by Ashesh Goplani
  </commit_message>
  <done>All Phase 2 acceptance criteria proven, full suite logs captured, no leaked tmux servers, SUMMARY written, phase signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new — verification-only plan) | Comment edits and test-run capture; no new code paths or trust crossings. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information disclosure | /tmp/phase2-final.log captures host paths and test names | accept | /tmp is local-only and the log is not committed (only the SUMMARY excerpts are). No secrets / no PII surfaces in test output. |
</threat_model>

<verification>
This plan IS the verification step. The acceptance criteria above ARE the verification.
</verification>

<success_criteria>
- Comment blocks updated.
- Full TestPersistence_ suite shows TEST-01..04 in their expected GREEN/SKIP states + new helpers all GREEN.
- TEST-05..08 RED is documented as expected (Phase 3 territory).
- No tmux servers leaked.
- SUMMARY captures full evidence + signs off Phase 2.
- Two commits, both signed `Committed by Ashesh Goplani`, no Claude attribution.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cgroup-isolation-default-req-1-fix/02-05-SUMMARY.md` with:
- Section "Phase 2 sign-off" listing the seven requirements + status
- Embedded `go test -v` output for all four test-runs (full)
- `systemctl --user status` evidence for an agentdeck-tmux-*.scope unit (or rationale for absence)
- `grep 'tmux cgroup isolation' ~/.agent-deck/logs/*.log` output (or, if logs dir is fresh, the captured stderr from the bootstrap call)
- The two commit SHAs from this plan
- A "Roadmap success criteria" table with each criterion proven/deferred
- `git add -f .planning/phases/02-cgroup-isolation-default-req-1-fix/02-05-SUMMARY.md`
</output>
